<div class="bookmark-list">
    @foreach (UrlBookmark b in bookmarks)
    {
        <UrlCard bookmark="b" />
    }
</div>

@code {
    [Parameter]
    public List<UrlBookmark> bookmarks { get; set; } = [];
}
@using UrlSaver.Domain;
@using UrlSaver.Components.UrlList.UrlCard;

<div class="d-flex flex-row flex-wrap gap-2">
    @foreach (UrlBookmark b in bookmarks)
    {
        <UrlCard bookmark="b" />
    }
</div>

@code {
    [Parameter]
    public List<UrlBookmark> bookmarks { get; set; } = [];
}